# CLI 实时音量条（VU meter） Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 `smoco run` 加 `--meter` 开关，开后在终端用 `\r` 持续刷新一条音量条，随采集到的音频电平起伏——没有 ASR 也能立刻确认采集通路正常。

**Architecture:** 在采集侧加一个 `MeteringSource` 装饰器（包任意 `AudioSource`，逐帧算 RMS 存 `latest_rms`，帧原样透传，pipeline 无感知）；CLI 开 `--meter` 时把源包一层、把 `smoco` 日志降到 WARNING，并用一个并发渲染协程读 `latest_rms`、每 50ms `\r` 重绘一条。电平计算 `frame_rms`、渲染 `meter_bar` 都是纯函数，全部 macOS 可单测。

**Tech Stack:** Python ≥3.10、numpy、argparse、asyncio、pytest。

---

## File Structure

| 文件 | 职责 |
|---|---|
| `smoco/audio.py` | 新增 `frame_rms(pcm, sample_width)` 纯函数 |
| `smoco/source/metering.py` | 新增 `MeteringSource` 装饰器 |
| `smoco/__main__.py` | 新增 `meter_bar`、`_render_meter`、`_run_with_meter`；加 `--meter`；改 `cmd_run` |
| `tests/test_audio.py` | 追加 `frame_rms` 单测 |
| `tests/test_metering.py` | `MeteringSource` / `meter_bar` / `_run_with_meter` 单测 |
| `README.md` | 追加真机清单 12–14 |

实现顺序：纯函数 → 装饰器 → 渲染条 → CLI 串联 → README。

---

## Task 1: `frame_rms` 纯函数

**Files:**
- Modify: `smoco/audio.py`
- Modify: `tests/test_audio.py`

- [ ] **Step 1: 追加失败测试到 `tests/test_audio.py`**

```python
def test_frame_rms_silence_is_zero():
    from smoco.audio import frame_rms
    pcm = np.zeros(480, dtype="<i2").tobytes()
    assert frame_rms(pcm) == 0.0


def test_frame_rms_full_scale_near_one():
    from smoco.audio import frame_rms
    pcm = np.full(480, 32767, dtype="<i2").tobytes()
    assert abs(frame_rms(pcm) - 1.0) < 1e-3


def test_frame_rms_known_constant():
    from smoco.audio import frame_rms
    val = int(0.5 * 32767)
    pcm = np.full(480, val, dtype="<i2").tobytes()
    assert abs(frame_rms(pcm) - (val / 32768.0)) < 1e-3


def test_frame_rms_empty_is_zero():
    from smoco.audio import frame_rms
    assert frame_rms(b"") == 0.0
```

（`np` 已在文件顶部 import。）

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_audio.py -q`
Expected: FAIL（`ImportError: cannot import name 'frame_rms'`）。

- [ ] **Step 3: 在 `smoco/audio.py` 末尾加 `frame_rms`**

```python
def frame_rms(pcm: bytes, sample_width: int = 2) -> float:
    """S16LE（默认）PCM 帧的 RMS 电平，归一化到 [0,1]。
    全静音 -> 0.0；满幅 -> ≈1.0；空数据 -> 0.0。"""
    if not pcm:
        return 0.0
    arr = np.frombuffer(pcm, dtype="<i2").astype(np.float32) / 32768.0
    return float(np.sqrt(np.mean(arr * arr)))
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_audio.py -q`
Expected: 既有 + 4 个新测试全过。

- [ ] **Step 5: 提交**

```bash
git add smoco/audio.py tests/test_audio.py
git commit -m "feat(audio): frame_rms 纯函数（S16LE 帧 RMS 电平）"
```

---

## Task 2: `MeteringSource` 装饰器

**Files:**
- Create: `smoco/source/metering.py`
- Create: `tests/test_metering.py`

- [ ] **Step 1: 写失败测试 `tests/test_metering.py`**

```python
import numpy as np
from smoco.audio import AudioFormat
from smoco.source.metering import MeteringSource


class _FakeSource:
    def __init__(self, frames, fmt):
        self._frames = list(frames)
        self._i = 0
        self.audio_format = fmt
        self.started = False
        self.stopped = False

    def start(self):
        self.started = True

    def stop(self):
        self.stopped = True

    def read_frame(self):
        if self._i >= len(self._frames):
            return None
        f = self._frames[self._i]
        self._i += 1
        return f


_FMT = AudioFormat()


def _full_frame():
    return np.full(_FMT.frame_samples(), 32767, dtype="<i2").tobytes()


def _silent_frame():
    return b"\x00" * _FMT.frame_bytes()


def test_metering_updates_rms_and_passes_through():
    src = _FakeSource([_full_frame(), _silent_frame()], _FMT)
    m = MeteringSource(src)
    m.start()
    assert src.started
    f1 = m.read_frame()
    assert f1 == _full_frame()              # 帧原样透传
    assert m.latest_rms > 0.9
    assert abs(m.latest_peak - m.latest_rms) < 1e-6
    m.read_frame()                          # 静音帧
    assert m.latest_rms == 0.0
    assert m.latest_peak > 0.9             # peak 不衰减
    assert m.read_frame() is None          # 源结束
    m.stop()
    assert src.stopped


def test_metering_delegates_audio_format():
    src = _FakeSource([], _FMT)
    m = MeteringSource(src)
    assert m.audio_format is _FMT


def test_metering_none_frame_keeps_levels():
    src = _FakeSource([], _FMT)
    m = MeteringSource(src)
    assert m.read_frame() is None
    assert m.latest_rms == 0.0
    assert m.latest_peak == 0.0
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_metering.py -q`
Expected: FAIL（`ImportError: No module named 'smoco.source.metering'`）。

- [ ] **Step 3: 写 `smoco/source/metering.py`**

```python
from __future__ import annotations
from .base import AudioSource
from ..audio import frame_rms


class MeteringSource:
    """装饰任意 AudioSource：透传帧，并实时更新 latest_rms / latest_peak。"""

    def __init__(self, inner: AudioSource):
        self._inner = inner
        self.latest_rms: float = 0.0
        self.latest_peak: float = 0.0

    @property
    def audio_format(self):
        return self._inner.audio_format

    def start(self) -> None:
        self._inner.start()

    def read_frame(self) -> bytes | None:
        frame = self._inner.read_frame()
        if frame:
            rms = frame_rms(frame, self._inner.audio_format.sample_width)
            self.latest_rms = rms
            if rms > self.latest_peak:
                self.latest_peak = rms
        return frame

    def stop(self) -> None:
        self._inner.stop()
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_metering.py -q`
Expected: `3 passed`。

- [ ] **Step 5: 提交**

```bash
git add smoco/source/metering.py tests/test_metering.py
git commit -m "feat(source): MeteringSource 装饰器（逐帧 RMS 透传）"
```

---

## Task 3: `meter_bar` 渲染纯函数

**Files:**
- Modify: `smoco/__main__.py`
- Modify: `tests/test_metering.py`

- [ ] **Step 1: 追加失败测试到 `tests/test_metering.py`**

```python
from smoco.__main__ import meter_bar


def test_meter_bar_empty():
    assert meter_bar(0.0) == "[" + "░" * 24 + "]"


def test_meter_bar_full():
    assert meter_bar(1.0) == "[" + "█" * 24 + "]"


def test_meter_bar_half():
    bar = meter_bar(0.5, width=24)
    assert bar.count("█") == 12
    assert bar.count("░") == 12


def test_meter_bar_clamps_out_of_range():
    assert meter_bar(-0.5) == "[" + "░" * 24 + "]"
    assert meter_bar(1.5) == "[" + "█" * 24 + "]"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_metering.py -q`
Expected: FAIL（`ImportError: cannot import name 'meter_bar'`）。

- [ ] **Step 3: 在 `smoco/__main__.py` 的 `pick_device` 之前加 `meter_bar`**

```python
def meter_bar(rms: float, width: int = 24) -> str:
    """把 [0,1] 电平渲染成条：[████████░░░░░░░░░░░░░░░░]"""
    level = max(0.0, min(1.0, rms))
    filled = int(round(level * width))
    return "[" + "█" * filled + "░" * (width - filled) + "]"
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_metering.py -q`
Expected: `7 passed`（3 + 4）。

- [ ] **Step 5: 提交**

```bash
git add smoco/__main__.py tests/test_metering.py
git commit -m "feat(cli): meter_bar 纯函数（电平渲染成条）"
```

---

## Task 4: 渲染协程 + `--meter` 串联

**Files:**
- Modify: `smoco/__main__.py`
- Modify: `tests/test_metering.py`

- [ ] **Step 1: 追加失败测试到 `tests/test_metering.py`**

```python
import asyncio
from smoco.__main__ import _run_with_meter


def test_run_with_meter_no_meter_just_runs_pipe():
    ran = []

    class FakePipe:
        error = None

        async def run(self):
            ran.append(True)

    asyncio.run(_run_with_meter(FakePipe(), None, meter=False))
    assert ran == [True]


def test_run_with_meter_renders_and_completes(capsys):
    class FakeSrc:
        audio_format = _FMT

        def start(self):
            pass

        def stop(self):
            pass

        def read_frame(self):
            return None

    class FakePipe:
        error = None

        async def run(self):
            await asyncio.sleep(0.1)   # 让渲染协程至少画一帧

    src = MeteringSource(FakeSrc())
    asyncio.run(_run_with_meter(FakePipe(), src, meter=True))
    out = capsys.readouterr().out
    assert "rms=" in out
    assert out.endswith("\n")           # 收尾换行


def test_cmd_run_file_meter_shows_bar(tmp_path, capsys):
    import soundfile as sf
    from smoco.__main__ import cmd_run, _build_parser

    wav = tmp_path / "a.wav"
    sf.write(str(wav),
             (np.random.RandomState(0).uniform(-0.5, 0.5, 16000)).astype("float32"),
             16000)
    args = _build_parser().parse_args(["run", "--file", str(wav), "--meter"])
    assert cmd_run(args) == 0
    assert "rms=" in capsys.readouterr().out
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_metering.py -q`
Expected: FAIL（`ImportError: cannot import name '_run_with_meter'`，且 `--meter` 参数不存在）。

- [ ] **Step 3: 加 `--meter` 参数**

在 `smoco/__main__.py` 的 `_build_parser` 里，`run` 子命令区，在 `--device` 之后加：

```python
    p_run.add_argument("--meter", action="store_true", help="显示实时音量条（确认采集通路）")
```

- [ ] **Step 4: 加 `_render_meter` 与 `_run_with_meter`**

在 `smoco/__main__.py` 的 `meter_bar` 之后加：

```python
async def _render_meter(source) -> None:
    """持续刷新音量条，直到被取消。"""
    while True:
        rms = source.latest_rms
        sys.stdout.write("\r" + meter_bar(rms) + f" rms={rms:.3f}  ")
        sys.stdout.flush()
        await asyncio.sleep(0.05)


async def _run_with_meter(pipe, source, meter: bool) -> None:
    """meter=False 时直接跑管线；True 时并发跑渲染，管线结束后收尾换行。"""
    if not meter:
        await pipe.run()
        return
    task = asyncio.create_task(_render_meter(source))
    try:
        await pipe.run()
    finally:
        task.cancel()
        try:
            await task
        except asyncio.CancelledError:
            pass
        sys.stdout.write("\n")
        sys.stdout.flush()
```

（`sys` 已在文件顶部 import。）

- [ ] **Step 5: 顶部 import `MeteringSource`**

在 `smoco/__main__.py` 顶部 import 区，`from .source.file import FileSource` 之后加：

```python
from .source.metering import MeteringSource
```

- [ ] **Step 6: 改 `cmd_run`，接入 meter**

把现有 `cmd_run` 中「构造 source」之后、「构造 vad」之前插入 meter 包装；并把 `asyncio.run(pipe.run())` 换成 `asyncio.run(_run_with_meter(pipe, source, args.meter))`。修改后的 `cmd_run` 完整为：

```python
def cmd_run(args) -> int:
    fmt = AudioFormat()
    cfg = Config()
    if args.wasapi:
        from .source.wasapi import WASAPILoopbackSource, _AVAILABLE
        if not _AVAILABLE:
            print("WASAPI 源不可用（需 Windows + pyaudiowpatch）")
            return 1
        device_index = _resolve_device(args, WASAPILoopbackSource.list_devices)
        if device_index is None:
            return 1
        source = WASAPILoopbackSource(target=fmt, device_index=device_index)
    elif args.file:
        source = FileSource(args.file, target=fmt)
    else:
        print("请指定 --file 或 --wasapi")
        return 1
    if args.meter:
        source = MeteringSource(source)
        logging.getLogger("smoco").setLevel(logging.WARNING)
    vad = WebRtcVad(aggressiveness=cfg.vad_aggressiveness, sample_rate=fmt.sample_rate)
    chunker = Chunker(vad, fmt, silence_ms=cfg.silence_ms,
                      max_chunk_ms=cfg.max_chunk_ms, min_chunk_ms=cfg.min_chunk_ms,
                      pad_ms=cfg.pad_ms)
    transcriber = StubTranscriber(out_dir=args.stub_out)
    pipe = Pipeline(source, chunker, transcriber, cfg)
    try:
        asyncio.run(_run_with_meter(pipe, source, args.meter))
    except KeyboardInterrupt:
        logging.getLogger("smoco").info("已中断")
        return 0
    if pipe.error is not None:
        logging.getLogger("smoco").error("采集源错误: %s", pipe.error)
        return 1
    return 0
```

（其余函数不动。注意：`pipe` 拿到的是包装后的 `source`，故采集线程经 `MeteringSource` 读帧、实时更新 `latest_rms`；`_run_with_meter` 收到的 `source` 也是包装后的，渲染协程读得到电平。）

- [ ] **Step 7: 运行测试确认通过**

Run: `pytest tests/test_metering.py -q`
Expected: `10 passed`（7 + 3）。

Run: `pytest -q`
Expected: 全量通过（既有 43 + 本特性新增，无失败）。

- [ ] **Step 8: 冒烟验证 `--help`**

Run: `python -m smoco run --help`
Expected: 列出 `--file`、`--wasapi`、`--stub-out`、`--device`、`--meter`，退出码 0。

- [ ] **Step 9: 提交**

```bash
git add smoco/__main__.py tests/test_metering.py
git commit -m "feat(cli): --meter 实时音量条（采集通路可视化）"
```

---

## Task 5: README 追加真机清单

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 在 README 的「Windows 真机验证清单」末尾追加 3 条**

在现有第 11 条之后追加：

```markdown
12. `smoco run --wasapi --meter` 选设备后，音量条随系统声音起伏；
13. 不播放声音时条很低（接近空）、播放时升高，证明采集通路正常；
14. Ctrl-C 后音量条干净收尾换行，无残留。
```

- [ ] **Step 2: 提交**

```bash
git add README.md
git commit -m "docs: README 追加音量条真机验证清单"
```

- [ ] **Step 3: 全量回归**

Run: `pytest -q`
Expected: 全部通过，无失败。

---

## Self-Review（plan 完成后自查记录）

- **Spec 覆盖**：`frame_rms`（Task 1）✓；`MeteringSource` 帧级 RMS 透传（Task 2）✓；`meter_bar`（Task 3）✓；`--meter` flag + 渲染协程 + `_run_with_meter` + `cmd_run` 包装源/降日志（Task 4）✓；并发与线程（采集线程写、渲染协程读，GIL 下不加锁）（Task 4）✓；日志降级（Task 4 cmd_run）✓；错误处理（None 帧不更新、Ctrl-C 收尾换行）（Task 2/4）✓；测试策略（纯函数 + FakeSource + cmd_run --file --meter 端到端，macOS 全绿）（Task 1–4）✓；Windows 清单（Task 5）✓。
- **占位符**：无 TBD/TODO；每步含完整代码与确切命令。
- **类型一致**：`frame_rms(pcm, sample_width=2)->float`、`MeteringSource(inner)`（latest_rms/latest_peak/audio_format/start/read_frame/stop）、`meter_bar(rms, width=24)->str`、`_render_meter(source)`、`_run_with_meter(pipe, source, meter)` 在各 Task 间签名一致；`cmd_run` 改动后既有 `test_cmd_run_*`（device-selection）仍通过——无 `--meter` 时走 `_run_with_meter(..., meter=False)` 直接 `await pipe.run()`，且 `pipe.error`/返回码逻辑不变。
